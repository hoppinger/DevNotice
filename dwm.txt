ruby deploy_testing.rb
stockholm.sweden
malmo.sweden

dwm

build --force-rm -f Dockerfile.testing -t #{image_tag} .
run -t #{image_tag} dotnet ef database update -c DrukwerkMaxContext
run --restart=always -d -p 0.0.0.0:#{expose_to_port}:5000 -t #{image_tag}


docker build --force-rm -f Dockerfile.testing -t dwm_t .

docker run -t dwm_t dotnet ef database update -c DrukwerkMaxContext

